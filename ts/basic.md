# Basic Knowledges

# Interface & Type

- Both can be used as types for objects, functions, etc.
- Interface declarations with the same name can be automatically merged.
- Type can use union types.

## Difference

- Extension: Interface uses the `extends` keyword, while Type directly uses `&`.
